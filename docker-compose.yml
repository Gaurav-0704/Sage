version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-haiku-4-5-20251001}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      # Optional: point at Postgres; defaults to SQLite inside the container.
      - DATABASE_URL=${DATABASE_URL:-}
      # Set SEED_DEMO=true to populate sample students/teachers/marks on first run.
      - SEED_DEMO=${SEED_DEMO:-false}
    volumes:
      - sage_db:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/')"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  sage_db:
